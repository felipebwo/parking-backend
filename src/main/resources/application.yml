spring:
  datasource:
    url: jdbc:mysql://localhost:3307/parking?createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
  main:
    allow-bean-definition-overriding: true

server:
  port: 3003

logging:
  level:
    root: INFO
    org.hibernate.SQL: DEBUG
